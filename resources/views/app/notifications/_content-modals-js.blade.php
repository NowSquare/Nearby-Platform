
  function selectDealInput(inp) {
    $('#select_deal_input').val(inp);
  }

  function selectDeal(url) {
    $('#' + $('#select_deal_input').val() + '').val(url);
    $('#dealSelectModalClose').trigger('click');
  }

  function selectCouponInput(inp) {
    $('#select_coupon_input').val(inp);
  }

  function selectCoupon(url) {
    $('#' + $('#select_coupon_input').val() + '').val(url);
    $('#couponSelectModalClose').trigger('click');
  }

  function selectRewardInput(inp) {
    $('#select_reward_input').val(inp);
  }

  function selectReward(url) {
    $('#' + $('#select_reward_input').val() + '').val(url);
    $('#rewardSelectModalClose').trigger('click');
  }

  function selectPropertyInput(inp) {
    $('#select_property_input').val(inp);
  }

  function selectProperty(url) {
    $('#' + $('#select_property_input').val() + '').val(url);
    $('#propertySelectModalClose').trigger('click');
  }

  function selectCardInput(inp) {
    $('#select_card_input').val(inp);
  }

  function selectCard(url) {
    $('#' + $('#select_card_input').val() + '').val(url);
    $('#cardSelectModalClose').trigger('click');
  }

  function selectVideoInput(inp) {
    $('#select_video_input').val(inp);
  }

  function selectVideo(url) {
    $('#' + $('#select_video_input').val() + '').val(url);
    $('#videoSelectModalClose').trigger('click');
  }

  function selectPageInput(inp) {
    $('#select_page_input').val(inp);
  }

  function selectPage(url) {
    $('#' + $('#select_page_input').val() + '').val(url);
    $('#pageSelectModalClose').trigger('click');
  }